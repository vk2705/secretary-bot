# b0a2398 — docs: start docs/commits/

**Date:** 2026-08-27
**Files:** `docs/commits/a81e9f4-docs-correct-stale-auth-03-note.md` (new)

## What changed

Created `docs/commits/` — one short markdown description per new commit,
going forward. Backfilled a doc for `a81e9f4` (the AUTH-03 note fix), which
was `HEAD` at the moment this convention started.

## Why

Requested directly: a per-commit description document, kept alongside the
repo, so the intent and reasoning behind each change is easy to find later
without digging through `git log` messages alone.

## Scope decision

Documenting starts from this point forward only — not backfilled across the
rest of the Milestone B branch history or the full repo history. Confirmed
with the user before proceeding.
